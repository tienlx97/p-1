# Frontend Rules

Before generating any styles, read:

- ./docs/stylex-authoring.md

## Styling Requirements

- Use StyleX only
- Never use inline styles
- Never use CSS modules
- Follow all rules in stylex-authoring.md
- Use stylex.create()
- Use longhand properties
- Use defineVars for theme values
- Use defineConsts for breakpoints
